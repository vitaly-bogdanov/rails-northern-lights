<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= yield :title %></title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= stylesheet_link_tag 'materialize' %>
    <%= stylesheet_link_tag 'client' %>
    <%= stylesheet_link_tag 'application' %>
    <%= javascript_include_tag 'materialize', defer: true %>
    <%= javascript_include_tag 'application', defer: true  %>
    <%= javascript_include_tag 'client', defer: true %>
    <meta name="description" content="<%= yield :description %>">
    <meta name="keywords" content="<%= yield :keywords %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body>
    <%= render 'includes/header_client' %>
    <%= yield :body %>
    <%= render 'includes/footer_client' %>
    <%= render 'includes/tap_target' %>
    <% unless current_page?(privacy_agreement_path) || current_page?(new_shop_order_path) %>
      <div id="bottom-modal-cart" class="modal bottom-sheet">
        <div class="container"></div>
      </div>
    <% end %>
  </body>
</html>
