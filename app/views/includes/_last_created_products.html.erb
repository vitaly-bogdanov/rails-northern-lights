<% unless @last_created_products.empty? %>
  <section class="section" id="will_like">
    <div class="container">
      <h2 class="section__title">Новинки</h2>
      <div class="products_grid products_grid--will_like">
        <% @last_created_products.each do |product| %>
          <% cache(product) do %>
            <%= render partial: 'includes/product_card', locals: { product_card: product } %>
          <% end %>
        <% end %>
      </div>
    </div>
  </section>
<% else %>
  <section class="section" id="will_like">
    <div class="container">
      <h2 class="section__title">Пока что тут пусто :-(</h2>
    </div>
  </section>
<% end %>